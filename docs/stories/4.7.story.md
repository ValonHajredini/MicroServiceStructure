# Story 4.7: Kanban UI - Board List & Board View

## Status
Approved

## Prerequisites

- [ ] **Expand @microservice/ui-common library** (HIGH PRIORITY - Blocks Stories 4.7-4.10)
  - Shared AuthService for token management and login state
  - Shared HTTP interceptor for JWT injection and error handling
  - Shared PrimeNG dialog wrappers (confirm, form, detail templates)
  - Shared PrimeNG toast/message service wrappers
  - Shared card components for standard layouts
- [ ] **Reference design-guide skill for PrimeNG patterns**
  - Use `/design-guide` skill for component implementation
  - Follow Trinity Pattern (standalone components, signals, @defer blocks)
- [ ] **Add JWT authentication flow for Angular app**
  - Login page redirects to Core UI auth
  - Token stored in localStorage/sessionStorage
  - HTTP interceptor injects Bearer token
  - Logout clears token

## Story

**As a** user,
**I want** an Angular app to view my Kanban boards,
**so that** I can access my task management system.

## Acceptance Criteria

1. Angular app at kanban.mydomain.com with board list view
2. Board list shows: name, description, last updated, number of tasks
3. "Create New Board" button opens creation modal
4. Clicking board navigates to /boards/:id with full board view
5. Board view displays columns horizontally with tasks as cards
6. Empty board state: "Add your first column" CTA
7. Responsive: Columns stack vertically on mobile, horizontal scroll on tablet

## Tasks / Subtasks

- [ ] Task 1: Set Up Kanban Angular Module (AC: 1)
  - [ ] Generate module: `ng g module features/kanban`
  - [ ] Create routing module: `ng g module features/kanban/kanban-routing`
  - [ ] Configure routes:
    - `/kanban` - board list (default)
    - `/kanban/boards/:id` - board view
  - [ ] Add lazy loading route in app-routing.module.ts

- [ ] Task 2: Create Board Service (AC: 2, 4)
  - [ ] Generate service: `ng g service features/kanban/services/board`
  - [ ] Method: `getBoards(): Observable<Board[]>`
    - Call GET /api/v1/boards
    - Return board list
  - [ ] Method: `getBoard(id: string): Observable<Board>`
    - Call GET /api/v1/boards/:id
    - Return board with columns and tasks
  - [ ] Method: `createBoard(board: CreateBoardDto): Observable<Board>`
    - Call POST /api/v1/boards
    - Return created board
  - [ ] Handle errors with error messages

- [ ] Task 3: Create Board List Component (AC: 2, 3)
  - [ ] Generate component: `ng g component features/kanban/board-list`
  - [ ] Display board cards in grid layout:
    - Board name (h2)
    - Description (p, truncated)
    - Last updated date (formatted)
    - Task count badge
  - [ ] Add "Create New Board" button (opens modal)
  - [ ] Add click handler: navigate to `/kanban/boards/:id`
  - [ ] Add loading state
  - [ ] Add empty state: "No boards yet. Create your first board!"

- [ ] Task 4: Create Board Creation Modal (AC: 3)
  - [ ] Generate component: `ng g component features/kanban/dialogs/create-board-dialog`
  - [ ] Use PrimeNG Dialog:
    - Form fields: name (required), description (optional)
    - Submit button: "Create Board"
    - Cancel button: "Cancel"
  - [ ] Form validation:
    - Name: required, max 255 chars
    - Description: optional, max 1000 chars
  - [ ] On submit: call boardService.createBoard()
  - [ ] On success: close dialog, navigate to new board
  - [ ] On error: show error message

- [ ] Task 5: Create Board View Component (AC: 4, 5, 6)
  - [ ] Generate component: `ng g component features/kanban/board-view`
  - [ ] Load board data on init (route param :id)
  - [ ] Display board header:
    - Board name (editable)
    - Board description (editable)
  - [ ] Display columns horizontally:
    - Use flexbox or CSS Grid
    - Each column rendered as separate component (deferred to Story 4.8)
  - [ ] Display tasks as cards within columns
  - [ ] Add empty state if no columns:
    - Icon: "pi-columns"
    - Message: "Add your first column"
    - CTA button: "Add Column" (deferred to Story 4.8)
  - [ ] Add loading state

- [ ] Task 6: Create Column Component (AC: 5)
  - [ ] Generate component: `ng g component features/kanban/column`
  - [ ] Display column header:
    - Column title
    - Task count badge
    - WIP limit indicator (if set)
  - [ ] Display tasks list:
    - Use ngFor to render task cards
    - Task card component (deferred to Story 4.8)
  - [ ] Add empty column state:
    - Message: "No tasks"
  - [ ] Styling: fixed width, scrollable if overflow

- [ ] Task 7: Implement Responsive Layout (AC: 7)
  - [ ] Desktop (>768px): Columns horizontal scroll
  - [ ] Tablet (768px-1024px): Columns horizontal scroll with scrollbar
  - [ ] Mobile (<768px): Columns stack vertically
  - [ ] Use CSS media queries or Angular BreakpointObserver
  - [ ] Test on multiple screen sizes

- [ ] Task 8: Add Unit Tests
  - [ ] Test BoardService methods
  - [ ] Test BoardListComponent:
    - Renders board cards
    - Opens create dialog
    - Navigates on click
  - [ ] Test BoardViewComponent:
    - Loads board data
    - Displays columns
    - Shows empty state
  - [ ] Test CreateBoardDialog component

- [ ] Task 9: Add Integration Tests
  - [ ] Test board list page load
  - [ ] Test board creation flow
  - [ ] Test board view navigation
  - [ ] Test responsive layout

## Dev Notes

### Architecture Context

**Frontend**: Angular 20  
[Source: architecture/system-architecture-overview.md#presentation-layer]

**UI Library**: PrimeNG  
[Source: Story 3.7 - Notes UI patterns]

**Service Integration**: Kanban Service API  
[Source: Stories 4.2-4.5]

### Previous Story Insights

**Story 4.2**: Board management API endpoints  
**Story 4.3**: Column management API  
**Story 4.4**: Task CRUD operations  
**Story 4.6**: Real-time updates (will integrate in Story 4.10)

### API Endpoints

**GET /api/v1/boards**  
Returns paginated board list

Response:
```json
{
  "success": true,
  "data": [
    {
      "id": "board-uuid",
      "name": "Project Alpha",
      "description": "Main project board",
      "created_at": "2025-01-14T10:00:00Z",
      "updated_at": "2025-01-14T12:00:00Z"
    }
  ],
  "meta": {
    "page": 1,
    "limit": 20,
    "total": 5
  }
}
```

**GET /api/v1/boards/:id**  
Returns board with columns and tasks

**POST /api/v1/boards**  
Create board

### Component Structure

```
features/kanban/
├── board-list/
│   ├── board-list.component.ts
│   ├── board-list.component.html
│   └── board-list.component.scss
├── board-view/
│   ├── board-view.component.ts
│   ├── board-view.component.html
│   └── board-view.component.scss
├── column/
│   ├── column.component.ts
│   ├── column.component.html
│   └── column.component.scss
├── dialogs/
│   └── create-board-dialog/
├── services/
│   └── board.service.ts
└── kanban-routing.module.ts
```

### Responsive Design

**Breakpoints**:
- Mobile: < 768px (columns stack vertically)
- Tablet: 768px - 1024px (horizontal scroll)
- Desktop: > 1024px (full horizontal layout)

### Testing Standards

[Source: Story 3.7 Testing Standards]

- Unit tests for components and services
- Integration tests for user flows
- Test responsive behavior
- Test error handling

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-14 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
_To be populated by Dev Agent_

### Debug Log References
_To be populated by Dev Agent_

### Completion Notes List
_To be populated by Dev Agent_

### File List
_To be populated by Dev Agent_

## QA Results
_To be populated by QA Agent_

