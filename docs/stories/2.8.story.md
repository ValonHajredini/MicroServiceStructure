# Story 2.8: User & Team Management UI

## Status
Draft

## Story

**As a** tenant admin,
**I want** to view and manage team members,
**so that** I can control who has access to my organization.

## Acceptance Criteria

1. GET /tenants/:id/users endpoint returns all users in tenant with roles
2. Team management page displays user list with: name, email, role, status
3. Admin can change user role (admin ↔ user) via PATCH /users/:id/role
4. Admin can remove user from tenant (soft delete) via DELETE /tenants/:id/users/:userId
5. Invite user button opens modal with invitation form
6. Pending join requests shown in separate tab/section with approve/reject actions
7. Non-admin users see read-only team list without management controls

## Tasks / Subtasks

- [ ] Task 1: Implement GET /tenants/:id/users Endpoint (AC: 1)
  - [ ] Create GET /api/v1/tenants/:id/users endpoint in TenantsController
  - [ ] Require JWT authentication
  - [ ] Verify user has access to tenant (admin or member)
  - [ ] Query users table joined with user_tenant_roles
  - [ ] Return user list with: id, email, firstName, lastName, role, status
  - [ ] Support pagination (page, limit query params)
  - [ ] Support filtering by role (query param: role=admin)
  - [ ] Order by created_at DESC

- [ ] Task 2: Implement PATCH /users/:id/role Endpoint (AC: 3)
  - [ ] Create PATCH /api/v1/users/:id/role endpoint in UsersController
  - [ ] Require JWT + admin role
  - [ ] Validate user and admin are in same tenant
  - [ ] Accept request body: { role: 'admin' | 'user' }
  - [ ] Update user_tenant_roles table with new role
  - [ ] Prevent user from removing their own admin role (business rule)
  - [ ] Return updated user with new role

- [ ] Task 3: Implement DELETE /tenants/:id/users/:userId Endpoint (AC: 4)
  - [ ] Create DELETE /api/v1/tenants/:id/users/:userId endpoint
  - [ ] Require JWT + admin role
  - [ ] Verify both user and admin in same tenant
  - [ ] Soft delete: update user status to 'inactive' in user_tenant_roles
  - [ ] Alternative: Delete user_tenant_roles record (hard delete from tenant)
  - [ ] Prevent admin from removing themselves (business rule)
  - [ ] Return success response

- [ ] Task 4: Create Team Management Component (AC: 2, 7)
  - [ ] Generate: `ng generate component features/admin/team-management`
  - [ ] Add route: `/admin/team` with AuthGuard
  - [ ] Fetch users: call GET /api/v1/tenants/:id/users
  - [ ] Display user table with columns: Name, Email, Role, Status, Actions
  - [ ] Show loading state while fetching
  - [ ] Handle empty state (no users)
  - [ ] Implement pagination controls if needed

- [ ] Task 5: Implement Role Management UI (AC: 3)
  - [ ] Add "Change Role" button/dropdown in each user row
  - [ ] Show only for admin users (hide from non-admins)
  - [ ] Open confirmation dialog before role change
  - [ ] Dialog shows: "Change [User Name]'s role to [New Role]?"
  - [ ] On confirm, call PATCH /api/v1/users/:id/role
  - [ ] Update UI optimistically or refetch user list
  - [ ] Show success/error notification

- [ ] Task 6: Implement User Removal UI (AC: 4)
  - [ ] Add "Remove" button in each user row (for admins only)
  - [ ] Open confirmation dialog before removal
  - [ ] Dialog shows: "Remove [User Name] from organization?"
  - [ ] On confirm, call DELETE /api/v1/tenants/:id/users/:userId
  - [ ] Remove user from UI list
  - [ ] Show success/error notification
  - [ ] Disable remove button for current user (can't remove self)

- [ ] Task 7: Integrate Invite User Modal (AC: 5)
  - [ ] Add "Invite User" button at top of team management page
  - [ ] Reuse invitation modal/component from Story 2.3
  - [ ] On successful invite, show success message
  - [ ] Optionally refresh user list to show pending invitation

- [ ] Task 8: Create Join Requests Tab/Section (AC: 6)
  - [ ] Add tabs: "Team Members" and "Pending Requests"
  - [ ] Pending Requests tab shows join requests from Story 2.4
  - [ ] Fetch: GET /api/v1/tenants/:id/join-requests
  - [ ] Display: User name, email, message, date requested
  - [ ] Add "Approve" and "Reject" buttons for each request
  - [ ] Approve: call PATCH /api/v1/tenants/join-requests/:id with action: 'approve'
  - [ ] Reject: show optional message input, call PATCH with action: 'reject'
  - [ ] Refresh lists after action

- [ ] Task 9: Implement Access Control (AC: 7)
  - [ ] Check user role from JWT
  - [ ] If non-admin: hide action buttons (role change, remove, invite)
  - [ ] If non-admin: show read-only view with message
  - [ ] If non-admin: hide "Pending Requests" tab
  - [ ] Ensure backend endpoints enforce admin-only access

- [ ] Task 10: Add User Status Indicators
  - [ ] Display user status badges (Active, Inactive, Pending)
  - [ ] Color coding: Active (green), Inactive (gray), Pending (yellow)
  - [ ] Filter by status (optional)

- [ ] Task 11: Add Unit Tests
  - [ ] Test GET /tenants/:id/users endpoint
  - [ ] Test role change endpoint and validation
  - [ ] Test user removal endpoint
  - [ ] Test admin-only access enforcement
  - [ ] Test frontend component rendering
  - [ ] Test access control (admin vs non-admin views)

## Dev Notes

### Architecture Context

**Service**: Core Service (Backend + Frontend)  
[Source: architecture/component-architecture.md#core-service]

**User Management**: Admins manage users within their tenant  
[Source: architecture/authentication-multi-tenancy.md#multi-tenancy-implementation]

**Database**: users, user_tenant_roles tables  
[Source: architecture/database-architecture.md#core-service-database-schema]

### Database Schema

**Existing Tables** (from Epic 1):

[Source: architecture/database-architecture.md#users, architecture/database-architecture.md#user_tenant_roles]

```sql
-- users table
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
    email VARCHAR(255) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    avatar_url TEXT,
    status VARCHAR(50) DEFAULT 'active',  -- active, inactive, suspended
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(tenant_id, email)
);

-- user_tenant_roles table
CREATE TABLE user_tenant_roles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
    role VARCHAR(50) NOT NULL,  -- admin, user, viewer
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(user_id, tenant_id, role)
);
```

**Query for User List**:
```sql
SELECT 
    u.id, 
    u.email, 
    u.first_name, 
    u.last_name,
    u.status,
    u.created_at,
    utr.role
FROM users u
JOIN user_tenant_roles utr ON u.id = utr.user_id
WHERE u.tenant_id = :tenantId
AND utr.tenant_id = :tenantId
ORDER BY u.created_at DESC;
```

### API Specifications

**1. GET /api/v1/tenants/:id/users**

[Source: AC 1]

**Authentication**: JWT required, tenant member required

**Query Parameters** (optional):
- `page` (default: 1)
- `limit` (default: 20)
- `role` (filter: admin, user)
- `status` (filter: active, inactive)

**Success Response (200)**:
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "email": "john@example.com",
      "firstName": "John",
      "lastName": "Doe",
      "role": "admin",
      "status": "active",
      "createdAt": "2025-10-01T10:00:00Z"
    },
    {
      "id": "uuid",
      "email": "jane@example.com",
      "firstName": "Jane",
      "lastName": "Smith",
      "role": "user",
      "status": "active",
      "createdAt": "2025-10-15T12:00:00Z"
    }
  ],
  "meta": {
    "page": 1,
    "limit": 20,
    "total": 5,
    "totalPages": 1
  }
}
```

**Error Responses**:
- 403: Not a member of this tenant
- 404: Tenant not found

**2. PATCH /api/v1/users/:id/role**

[Source: AC 3]

**Authentication**: JWT required, admin role required

**Request**:
```json
{
  "role": "admin"
}
```

**Success Response (200)**:
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "email": "jane@example.com",
    "firstName": "Jane",
    "lastName": "Smith",
    "role": "admin",
    "updatedAt": "2025-10-29T10:00:00Z"
  }
}
```

**Error Responses**:
- 400: Invalid role
- 403: Not an admin or not in same tenant
- 409: Cannot change own role
- 404: User not found

**3. DELETE /api/v1/tenants/:id/users/:userId**

[Source: AC 4]

**Authentication**: JWT required, admin role required

**Success Response (200)**:
```json
{
  "success": true,
  "data": {
    "message": "User removed from organization",
    "userId": "uuid"
  }
}
```

**Error Responses**:
- 403: Not an admin
- 404: User not found
- 409: Cannot remove yourself

### Backend Structure

**Files to Create/Update**:
```
core-api/src/
├── tenants/
│   ├── tenants.controller.ts (add GET /:id/users, DELETE /:id/users/:userId)
│   ├── tenants.service.ts (add getUsersInTenant, removeUser)
├── users/
│   ├── users.controller.ts (add PATCH /:id/role)
│   ├── users.service.ts (add changeRole)
│   ├── dto/
│   │   ├── change-role.dto.ts
│   │   └── user-response.dto.ts
```

### Frontend Structure

**Components to Create**:
```
apps/core-ui/src/app/features/admin/
├── team-management/
│   ├── team-management.component.ts
│   ├── team-management.component.html
│   ├── team-management.component.scss
│   ├── team-management.component.spec.ts
│   ├── user-list/
│   │   ├── user-list.component.ts
│   │   ├── user-list.component.html
│   │   └── user-list.component.scss
│   ├── join-requests-list/
│   │   └── (reuse from Story 2.4)
│   └── dialogs/
│       ├── change-role-dialog.component.ts
│       ├── remove-user-dialog.component.ts
│       └── invite-user-dialog.component.ts (reuse from Story 2.3)
```

**Services to Create/Update**:
- `team.service.ts` - Handle team management API calls
- `users.service.ts` - User-related operations

**Routing**:
- `/admin/team` - Team management page (admin only)

### Team Management Component Implementation

[Source: AC 2, 7]

```typescript
// team-management.component.ts
@Component({
  selector: 'app-team-management',
  templateUrl: './team-management.component.html',
  styleUrls: ['./team-management.component.scss']
})
export class TeamManagementComponent implements OnInit {
  users: User[] = [];
  joinRequests: JoinRequest[] = [];
  loading = true;
  isAdmin = false;
  currentUserId = '';
  selectedTab = 0;

  constructor(
    private teamService: TeamService,
    private authService: AuthService,
    private dialog: MatDialog,
    private snackBar: MatSnackBar
  ) {}

  ngOnInit() {
    this.checkAdminRole();
    this.loadUsers();
    if (this.isAdmin) {
      this.loadJoinRequests();
    }
  }

  checkAdminRole() {
    const user = this.authService.getCurrentUser();
    this.currentUserId = user?.id;
    this.isAdmin = user?.roles?.includes('admin');
  }

  async loadUsers() {
    this.loading = true;
    try {
      this.users = await this.teamService.getTeamMembers();
    } catch (error) {
      this.snackBar.open('Error loading team members', 'Close', { duration: 3000 });
    } finally {
      this.loading = false;
    }
  }

  async loadJoinRequests() {
    this.joinRequests = await this.teamService.getJoinRequests();
  }

  openInviteDialog() {
    const dialogRef = this.dialog.open(InviteUserDialogComponent);
    dialogRef.afterClosed().subscribe(result => {
      if (result) {
        this.loadUsers();
      }
    });
  }

  openChangeRoleDialog(user: User) {
    const dialogRef = this.dialog.open(ChangeRoleDialogComponent, {
      data: { user }
    });
    dialogRef.afterClosed().subscribe(async (newRole) => {
      if (newRole) {
        await this.changeUserRole(user, newRole);
      }
    });
  }

  async changeUserRole(user: User, newRole: string) {
    try {
      await this.teamService.changeUserRole(user.id, newRole);
      this.snackBar.open('User role updated', 'Close', { duration: 3000 });
      this.loadUsers();
    } catch (error) {
      this.snackBar.open('Error updating role', 'Close', { duration: 3000 });
    }
  }

  openRemoveDialog(user: User) {
    const dialogRef = this.dialog.open(RemoveUserDialogComponent, {
      data: { user }
    });
    dialogRef.afterClosed().subscribe(async (confirmed) => {
      if (confirmed) {
        await this.removeUser(user);
      }
    });
  }

  async removeUser(user: User) {
    try {
      await this.teamService.removeUser(user.id);
      this.snackBar.open('User removed from organization', 'Close', { duration: 3000 });
      this.users = this.users.filter(u => u.id !== user.id);
    } catch (error) {
      this.snackBar.open('Error removing user', 'Close', { duration: 3000 });
    }
  }

  isCurrentUser(userId: string): boolean {
    return userId === this.currentUserId;
  }
}
```

### User List Template

[Source: AC 2, 3, 4, 7]

```html
<!-- team-management.component.html -->
<div class="team-management-container">
  <div class="header">
    <h1>Team Management</h1>
    <button *ngIf="isAdmin" 
            mat-raised-button 
            color="primary"
            (click)="openInviteDialog()">
      <mat-icon>person_add</mat-icon>
      Invite User
    </button>
  </div>

  <mat-tab-group [(selectedIndex)]="selectedTab">
    <!-- Team Members Tab -->
    <mat-tab label="Team Members">
      <div class="tab-content">
        <div *ngIf="!isAdmin" class="info-message">
          <mat-icon>info</mat-icon>
          <p>You are viewing team members in read-only mode.</p>
        </div>

        <table mat-table [dataSource]="users" class="users-table">
          <!-- Name Column -->
          <ng-container matColumnDef="name">
            <th mat-header-cell *matHeaderCellDef>Name</th>
            <td mat-cell *matCellDef="let user">
              {{ user.firstName }} {{ user.lastName }}
            </td>
          </ng-container>

          <!-- Email Column -->
          <ng-container matColumnDef="email">
            <th mat-header-cell *matHeaderCellDef>Email</th>
            <td mat-cell *matCellDef="let user">{{ user.email }}</td>
          </ng-container>

          <!-- Role Column -->
          <ng-container matColumnDef="role">
            <th mat-header-cell *matHeaderCellDef>Role</th>
            <td mat-cell *matCellDef="let user">
              <span class="role-badge" [class]="user.role">
                {{ user.role | titlecase }}
              </span>
            </td>
          </ng-container>

          <!-- Status Column -->
          <ng-container matColumnDef="status">
            <th mat-header-cell *matHeaderCellDef>Status</th>
            <td mat-cell *matCellDef="let user">
              <span class="status-badge" [class]="user.status">
                {{ user.status | titlecase }}
              </span>
            </td>
          </ng-container>

          <!-- Actions Column -->
          <ng-container matColumnDef="actions">
            <th mat-header-cell *matHeaderCellDef>Actions</th>
            <td mat-cell *matCellDef="let user">
              <button *ngIf="isAdmin && !isCurrentUser(user.id)"
                      mat-icon-button
                      [matMenuTriggerFor]="userMenu">
                <mat-icon>more_vert</mat-icon>
              </button>
              <mat-menu #userMenu="matMenu">
                <button mat-menu-item (click)="openChangeRoleDialog(user)">
                  <mat-icon>swap_horiz</mat-icon>
                  <span>Change Role</span>
                </button>
                <button mat-menu-item (click)="openRemoveDialog(user)">
                  <mat-icon>person_remove</mat-icon>
                  <span>Remove User</span>
                </button>
              </mat-menu>
            </td>
          </ng-container>

          <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
          <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
        </table>

        <div *ngIf="users.length === 0" class="empty-state">
          <mat-icon>group</mat-icon>
          <p>No team members found</p>
        </div>
      </div>
    </mat-tab>

    <!-- Pending Requests Tab (Admin Only) -->
    <mat-tab label="Pending Requests" *ngIf="isAdmin">
      <div class="tab-content">
        <app-join-requests-list 
          [requests]="joinRequests"
          (approved)="loadJoinRequests()"
          (rejected)="loadJoinRequests()">
        </app-join-requests-list>
      </div>
    </mat-tab>
  </mat-tab-group>
</div>
```

### Change Role Dialog

[Source: AC 3]

```html
<!-- change-role-dialog.component.html -->
<h2 mat-dialog-title>Change User Role</h2>

<mat-dialog-content>
  <p>
    Change <strong>{{ data.user.firstName }} {{ data.user.lastName }}</strong>'s 
    role to:
  </p>
  
  <mat-radio-group [(ngModel)]="selectedRole">
    <mat-radio-button value="admin">
      <strong>Admin</strong> - Full access to manage team and settings
    </mat-radio-button>
    <mat-radio-button value="user">
      <strong>User</strong> - Standard access to services
    </mat-radio-button>
  </mat-radio-group>
</mat-dialog-content>

<mat-dialog-actions align="end">
  <button mat-button mat-dialog-close>Cancel</button>
  <button mat-raised-button 
          color="primary"
          [mat-dialog-close]="selectedRole">
    Change Role
  </button>
</mat-dialog-actions>
```

### Remove User Dialog

[Source: AC 4]

```html
<!-- remove-user-dialog.component.html -->
<h2 mat-dialog-title>Remove User</h2>

<mat-dialog-content>
  <p>
    Are you sure you want to remove 
    <strong>{{ data.user.firstName }} {{ data.user.lastName }}</strong> 
    from your organization?
  </p>
  <p class="warning-text">
    <mat-icon>warning</mat-icon>
    This action cannot be undone. The user will lose access to all services.
  </p>
</mat-dialog-content>

<mat-dialog-actions align="end">
  <button mat-button mat-dialog-close>Cancel</button>
  <button mat-raised-button 
          color="warn"
          [mat-dialog-close]="true">
    Remove User
  </button>
</mat-dialog-actions>
```

### Business Logic Rules

[Source: AC 3, 4, derived requirements]

1. **Self-Protection**: Admin cannot change their own role or remove themselves
2. **Role Validation**: Only 'admin' and 'user' roles allowed
3. **Tenant Isolation**: Users can only manage users in their own tenant
4. **Admin Authority**: Only tenant admins can modify roles or remove users
5. **Last Admin Prevention**: Prevent removal of last admin (optional but recommended)
6. **Cascade Delete**: When user removed from tenant, delete user_tenant_roles entry

### Authorization Guards

[Source: AC 7, architecture/authentication-multi-tenancy.md]

**Backend Validation**:
```typescript
@Patch('users/:id/role')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('admin')
async changeUserRole(
  @Param('id') userId: string,
  @Body() dto: ChangeRoleDto,
  @CurrentUser() admin: any
) {
  // Prevent changing own role
  if (userId === admin.userId) {
    throw new ConflictException('Cannot change your own role');
  }

  // Verify user is in same tenant
  const user = await this.usersService.findOne(userId);
  if (user.tenantId !== admin.tenantId) {
    throw new ForbiddenException('User not in your organization');
  }

  return this.usersService.changeRole(userId, dto.role);
}
```

### Access Control in Frontend

[Source: AC 7]

```typescript
// Component logic
checkAdminRole() {
  const user = this.authService.getCurrentUser();
  this.isAdmin = user?.roles?.includes('admin');
  
  if (!this.isAdmin) {
    // Hide admin actions, show read-only view
    this.displayedColumns = ['name', 'email', 'role', 'status'];
  } else {
    this.displayedColumns = ['name', 'email', 'role', 'status', 'actions'];
  }
}
```

### User Status Badges

[Source: AC 2]

```scss
// team-management.component.scss
.role-badge {
  padding: 0.25rem 0.75rem;
  border-radius: 12px;
  font-size: 0.75rem;
  font-weight: 600;
  
  &.admin {
    background: #e3f2fd;
    color: #1565c0;
  }
  
  &.user {
    background: #f3e5f5;
    color: #6a1b9a;
  }
}

.status-badge {
  padding: 0.25rem 0.75rem;
  border-radius: 12px;
  font-size: 0.75rem;
  font-weight: 600;
  
  &.active {
    background: #e8f5e9;
    color: #2e7d32;
  }
  
  &.inactive {
    background: #eeeeee;
    color: #616161;
  }
  
  &.pending {
    background: #fff9c4;
    color: #f57f17;
  }
}
```

### Integration with Previous Stories

**Story 2.3 (Invitations)**: Reuse invite modal component  
**Story 2.4 (Join Requests)**: Reuse join requests list component

### Error Handling

**Backend Errors**:
- Cannot remove self: "You cannot remove yourself from the organization"
- Cannot change own role: "You cannot change your own role"
- User not found: "User not found in your organization"
- Not admin: "Only administrators can manage team members"

**Frontend Errors**:
- Load failed: "Unable to load team members. Please try again."
- Role change failed: "Error updating user role. Please try again."
- Remove failed: "Error removing user. Please try again."

### Security Considerations

[Source: architecture/security-architecture.md]

1. **Admin-Only Access**: Enforce on backend and frontend
2. **Tenant Isolation**: Users can only see/manage users in their tenant
3. **Self-Protection**: Prevent users from harming their own access
4. **Audit Trail**: Log all role changes and removals (future enhancement)
5. **Rate Limiting**: Prevent bulk user manipulation

### Testing Strategy

**Unit Tests**:
- GET users endpoint returns correct data
- Role change validates admin access
- Remove user prevents self-removal
- Frontend component renders correctly
- Access control logic works (admin vs non-admin)

**Integration Tests**:
- Full user management flow: list → change role → remove
- Admin-only enforcement
- Tenant isolation

**E2E Tests** (optional):
- Admin invites user → changes role → removes user

### Important Notes

1. **Last Admin**: Consider preventing removal of last admin in tenant
2. **Audit Log**: Consider logging all team management actions (future)
3. **Email Notifications**: Consider notifying users when role changed or removed (future)
4. **Bulk Operations**: Not in scope, but consider for future (bulk role changes, bulk removes)
5. **User Permissions**: Currently only admin/user roles, consider adding more granular permissions in future

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-29 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results

*To be filled by QA agent*

