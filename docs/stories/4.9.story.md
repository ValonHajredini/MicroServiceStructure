# Story 4.9: Kanban UI - Task Detail & Comments

## Status
Approved

## Story

**As a** user,
**I want** to view task details and add comments,
**so that** I can collaborate on work items.

## Acceptance Criteria

1. Clicking task card opens detail modal/sidebar
2. Task detail shows: title (editable inline), description, assignee dropdown, priority selector, due date picker
3. Comments section displays existing comments with author, timestamp
4. Comment input field with "Add comment" button
5. Activity timeline shows task history: created by X, moved to Y, assigned to Z
6. "Delete task" button in modal with confirmation
7. Modal closable via X button, Esc key, or clicking outside

## Tasks / Subtasks

- [ ] Task 1: Create Task Detail Modal Component (AC: 1, 7)
  - [ ] Generate component: `ng g component features/kanban/dialogs/task-detail-dialog`
  - [ ] Use PrimeNG Dialog:
    - Header: Task title (editable)
    - Content: Task details form, comments, activity
    - Footer: Delete button, Close button
  - [ ] Add dialog configuration:
    - Modal: true
    - DismissableMask: true (close on outside click)
    - Closable: true (X button)
    - Style: fullscreen on mobile, centered on desktop
  - [ ] Handle close events:
    - X button click
    - Escape key press
    - Outside click (dismissableMask)
  - [ ] Pass task data via dialog data

- [ ] Task 2: Create Task Detail Form (AC: 2)
  - [ ] Title field (inline editable):
    - Input field or contenteditable div
    - Save on blur or Enter key
    - Call API: PATCH /tasks/:id
  - [ ] Description field:
    - Textarea
    - Auto-save on blur or manual save button
    - Call API: PATCH /tasks/:id
  - [ ] Assignee dropdown:
    - Load users from tenant (may need Core Service API)
    - PrimeNG dropdown component
    - Display avatar + name
    - Save on selection change
    - Call API: PATCH /tasks/:id
  - [ ] Priority selector:
    - PrimeNG select button or radio buttons
    - Options: Low, Medium, High
    - Visual indicators (colors/icons)
    - Save on selection change
    - Call API: PATCH /tasks/:id
  - [ ] Due date picker:
    - PrimeNG calendar component
    - Display formatted date
    - Clear button to remove due date
    - Save on date change
    - Call API: PATCH /tasks/:id

- [ ] Task 3: Create Comments Section (AC: 3, 4)
  - [ ] Display comments list:
    - Use ngFor to render comment components
    - Order by created_at ASC
    - Show author avatar/name
    - Show timestamp (relative: "2 hours ago")
    - Show comment content
  - [ ] Create comment input:
    - Textarea for comment text
    - Character counter (max 5000)
    - "Add comment" button
    - Disable button if empty
  - [ ] On submit:
    - Call API: POST /tasks/:id/comments
    - Add comment to list immediately (optimistic)
    - Clear input field
    - If error: revert and show error message

- [ ] Task 4: Create Comment Component (AC: 3)
  - [ ] Generate component: `ng g component features/kanban/comment`
  - [ ] Display:
    - Author avatar (or initials)
    - Author name
    - Timestamp (formatted)
    - Comment content
  - [ ] Add edit/delete buttons (if author or admin):
    - Edit: inline edit mode
    - Delete: confirmation dialog
  - [ ] Styling: comment card design

- [ ] Task 5: Create Activity Timeline Component (AC: 5)
  - [ ] Generate component: `ng g component features/kanban/activity-timeline`
  - [ ] Load activity data:
    - Call API: GET /tasks/:id/activity
    - Order by created_at DESC (newest first)
  - [ ] Display activity items:
    - Icon based on action type
    - Action description: "Created by John", "Moved to In Progress", "Assigned to Jane"
    - Timestamp (relative)
  - [ ] Visual timeline design:
    - Vertical line connecting activities
    - Icons/avatars for each activity
    - Color coding by action type

- [ ] Task 6: Add Task Service Methods (AC: 2, 4)
  - [ ] Update task.service.ts
  - [ ] Method: `updateTask(id: string, updates: UpdateTaskDto): Observable<Task>`
    - Call PATCH /api/v1/tasks/:id
    - Return updated task
  - [ ] Method: `getTask(id: string): Observable<Task>`
    - Call GET /api/v1/tasks/:id
    - Return task with comments
  - [ ] Method: `deleteTask(id: string): Observable<void>`
    - Call DELETE /api/v1/tasks/:id
  - [ ] Create comment.service.ts:
    - Method: `addComment(taskId: string, content: string): Observable<TaskComment>`
      - Call POST /api/v1/tasks/:taskId/comments
    - Method: `updateComment(commentId: string, content: string): Observable<TaskComment>`
      - Call PATCH /api/v1/comments/:id
    - Method: `deleteComment(commentId: string): Observable<void>`
      - Call DELETE /api/v1/comments/:id
  - [ ] Create activity.service.ts:
    - Method: `getActivity(taskId: string): Observable<TaskActivity[]>`
      - Call GET /api/v1/tasks/:id/activity

- [ ] Task 7: Add Delete Confirmation (AC: 6)
  - [ ] Use PrimeNG ConfirmationService
  - [ ] Show confirmation dialog:
    - Message: "Delete this task? This action cannot be undone."
    - Header: "Confirm Delete"
    - Accept: "Delete"
    - Reject: "Cancel"
  - [ ] On confirm: call taskService.deleteTask()
  - [ ] On success: close modal, refresh board view
  - [ ] On error: show error message

- [ ] Task 8: Add Unit Tests
  - [ ] Test TaskDetailDialogComponent:
    - Opens with task data
    - Closes on X/Escape/outside click
    - Updates task fields
  - [ ] Test CommentsComponent:
    - Displays comments
    - Adds new comment
    - Edits/deletes comment (if authorized)
  - [ ] Test ActivityTimelineComponent:
    - Displays activity items
    - Formats timestamps correctly
  - [ ] Test task service methods

- [ ] Task 9: Add Integration Tests
  - [ ] Test task detail modal open/close
  - [ ] Test task field updates
  - [ ] Test comment creation
  - [ ] Test task deletion with confirmation
  - [ ] Test activity timeline display

## Dev Notes

### Architecture Context

**Frontend**: Angular 20  
[Source: architecture/system-architecture-overview.md#presentation-layer]

**UI Library**: PrimeNG  
[Source: Story 3.7 - Notes UI patterns]

**API Integration**: Kanban Service API  
[Source: Stories 4.4, 4.5]

### Previous Story Insights

**Story 4.4**: Task CRUD API endpoints  
**Story 4.5**: Comments and activity API endpoints  
**Story 4.7**: Board view component structure  
**Story 4.8**: Task card component

### API Endpoints

**GET /api/v1/tasks/:id**  
[Source: Story 4.4]

Returns task with comments

**PATCH /api/v1/tasks/:id**  
[Source: Story 4.4]

Update task fields

**POST /api/v1/tasks/:taskId/comments**  
[Source: Story 4.5]

Create comment

**GET /api/v1/tasks/:id/activity**  
[Source: Story 4.5]

Get activity timeline

**DELETE /api/v1/tasks/:id**  
[Source: Story 4.4]

Soft delete task

### Modal Implementation

**PrimeNG Dialog**:
```typescript
// task-detail-dialog.component.ts
constructor(private dialogRef: DynamicDialogRef, public config: DynamicDialogConfig) {}

ngOnInit() {
  this.task = this.config.data.task;
}

close() {
  this.dialogRef.close();
}

// Usage in board-view.component.ts
openTaskDetail(task: Task) {
  const ref = this.dialogService.open(TaskDetailDialogComponent, {
    data: { task },
    header: task.title,
    width: '90%',
    maximizable: true
  });
}
```

### Inline Editing Pattern

**Title Inline Edit**:
```typescript
editingTitle = false;

startEditTitle() {
  this.editingTitle = true;
}

saveTitle() {
  this.taskService.updateTask(this.task.id, { title: this.task.title })
    .subscribe(() => {
      this.editingTitle = false;
    });
}
```

### Activity Timeline Format

**Activity Items**:
- "Created by John Doe" (2 hours ago)
- "Assigned to Jane Smith" (1 hour ago)
- "Moved to In Progress" (30 minutes ago)
- "Comment added by John Doe" (15 minutes ago)

### Testing Standards

[Source: Story 4.7 Testing Standards]

- Unit tests for components
- Integration tests for user flows
- Test modal interactions
- Test form validation
- Test comment CRUD operations

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-14 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used
_To be populated by Dev Agent_

### Debug Log References
_To be populated by Dev Agent_

### Completion Notes List
_To be populated by Dev Agent_

### File List
_To be populated by Dev Agent_

## QA Results
_To be populated by QA Agent_

