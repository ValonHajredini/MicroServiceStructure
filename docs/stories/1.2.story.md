# Story 1.2: POC - JWT SSO Pattern Validation

## Status
Approved

## Story

**As a** developer,
**I want** to validate JWT-based single sign-on across multiple services,
**so that** we prove the authentication pattern before building full platform.

## Acceptance Criteria

1. Minimal NestJS auth-service issues JWT tokens containing userId, tenantId, roles claims
2. Minimal NestJS resource-service validates JWT and extracts tenant context
3. Basic Angular app demonstrates: login → receive token → access both services with same token
4. JWT signing uses chosen strategy (HS256 or RS256) decided during POC
5. Token validation middleware implemented in resource-service protecting endpoints
6. POC documented in docs/pocs/jwt-sso-pattern.md with code examples
7. Decision recorded: HS256 vs RS256 choice with rationale in ADR-001

## Tasks / Subtasks

- [ ] **Task 1: Create POC directory structure** (AC: 6)
  - [ ] Create docs/pocs/ directory
  - [ ] Set up workspace for POC code (separate from main apps)
  - [ ] Create poc-auth-service/ directory
  - [ ] Create poc-resource-service/ directory
  - [ ] Create poc-client/ directory for Angular demo

- [ ] **Task 2: Build minimal NestJS auth-service** (AC: 1, 4)
  - [ ] Initialize minimal NestJS application in poc-auth-service/
  - [ ] Install dependencies: @nestjs/jwt, @nestjs/passport, passport-jwt, bcrypt
  - [ ] Create POST /auth/login endpoint
  - [ ] Implement JWT token generation with claims: userId, tenantId, roles
  - [ ] Test both HS256 and RS256 signing strategies
  - [ ] Document performance and complexity differences

- [ ] **Task 3: Build minimal NestJS resource-service** (AC: 2, 5)
  - [ ] Initialize minimal NestJS application in poc-resource-service/
  - [ ] Install JWT validation dependencies
  - [ ] Implement JwtStrategy using Passport
  - [ ] Create JWT validation middleware
  - [ ] Create protected endpoint: GET /protected/resource
  - [ ] Extract and log tenantId from JWT payload
  - [ ] Verify middleware blocks requests without valid JWT

- [ ] **Task 4: Create basic Angular demo client** (AC: 3)
  - [ ] Initialize minimal Angular application in poc-client/
  - [ ] Create simple login form (hardcoded test credentials)
  - [ ] Implement login flow: call auth-service, store JWT in localStorage
  - [ ] Create service to add Authorization header to HTTP requests
  - [ ] Demonstrate calling both auth-service and resource-service with same token
  - [ ] Display decoded JWT payload in UI

- [ ] **Task 5: Test SSO across services** (AC: 3, 5)
  - [ ] Verify token issued by auth-service works in resource-service
  - [ ] Test token validation with valid JWT
  - [ ] Test rejection of invalid/expired tokens
  - [ ] Test extraction of tenantId and other claims
  - [ ] Document any issues or limitations discovered

- [ ] **Task 6: Make HS256 vs RS256 decision** (AC: 4, 7)
  - [ ] Compare implementation complexity of both approaches
  - [ ] Test token generation/validation performance
  - [ ] Evaluate key management requirements
  - [ ] Assess scalability implications for microservices
  - [ ] Document recommendation with pros/cons

- [ ] **Task 7: Document POC findings** (AC: 6)
  - [ ] Create docs/pocs/jwt-sso-pattern.md
  - [ ] Include code examples from POC implementation
  - [ ] Document JWT token structure and claims
  - [ ] Document validation flow across services
  - [ ] Include lessons learned and gotchas
  - [ ] Add recommendations for production implementation

- [ ] **Task 8: Update ADR-001 with decision** (AC: 7)
  - [ ] Update docs/architecture/architecture-decision-records.md
  - [ ] Record chosen strategy (HS256 or RS256)
  - [ ] Include rationale based on POC findings
  - [ ] Document trade-offs and future considerations
  - [ ] Note any caveats or security considerations

## Dev Notes

### Architecture Context

This is a **Proof of Concept (POC)** story - the goal is to validate the JWT SSO pattern BEFORE building the production authentication service. Code from this POC will inform Story 1.4 but is not intended for production use.

**Key Architecture Decisions to Validate:**
- ADR-003: JWT with HS256 Signing (Phase 1) - This POC will confirm or modify this decision [Source: architecture/architecture-decision-records.md#adr-003]

### Previous Story Context

**Story 1.1 Completed:**
- Turborepo monorepo structure is now in place
- Workspace directories: apps/, libs/, docs/
- Shared-types library exists with JwtPayload interface defined
- Build tooling (turbo.json) configured

**Important:** This POC should be built OUTSIDE the main workspace structure to avoid polluting production code. Create POC workspaces separately.

### JWT SSO Pattern Overview

[Source: architecture/authentication-multi-tenancy.md#jwt-based-sso-flow]

**The pattern to validate:**

1. **Login Flow:**
   - User submits credentials to auth-service
   - Auth-service validates and generates JWT with claims
   - JWT returned to client, stored in localStorage

2. **Service Access Flow:**
   - Client includes JWT in Authorization header: `Bearer <token>`
   - Each service validates JWT using shared secret (HS256) or public key (RS256)
   - Service extracts tenantId and user context from validated token

3. **Token Structure:**
```typescript
{
  sub: "user-uuid",           // User ID
  email: "user@example.com",
  tenantId: "tenant-uuid",
  roles: ["admin"],
  enabledServices: ["notes", "kanban"],
  iat: 1234567890,           // Issued at
  exp: 1234654290            // Expires (24 hours)
}
```

### JWT Validation Implementation

[Source: architecture/authentication-multi-tenancy.md#jwt-based-sso-flow]

**JwtStrategy Pattern (to be implemented in POC):**

```typescript
@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor() {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: process.env.JWT_SECRET  // HS256 approach
      // OR for RS256:
      // secretOrKeyProvider: (request, rawJwtToken, done) => {
      //   done(null, PUBLIC_KEY);
      // }
    });
  }

  async validate(payload: JwtPayload) {
    return {
      userId: payload.sub,
      email: payload.email,
      tenantId: payload.tenantId,
      roles: payload.roles,
      enabledServices: payload.enabledServices
    };
  }
}
```

### HS256 vs RS256 Decision Criteria

[Source: architecture/authentication-multi-tenancy.md#security-considerations]

**HS256 (Symmetric):**
- ✅ Simpler implementation
- ✅ Faster token generation/validation
- ⚠️ Shared secret across all services
- ⚠️ Secret rotation affects all services simultaneously

**RS256 (Asymmetric):**
- ✅ Only auth-service needs private key
- ✅ Other services only need public key
- ✅ Better security model for microservices
- ⚠️ More complex key management
- ⚠️ Slightly slower performance

**POC Must Test:**
- Implementation complexity for both approaches
- Performance differences (token generation and validation)
- Key distribution and management requirements
- Suitability for our microservices architecture

### Security Considerations

[Source: architecture/security-architecture.md#security-checklist]

**JWT Security Requirements:**
- Token expiration: 24 hours (configurable via env var)
- Secret management: Use environment variables, NEVER hardcode
- Algorithm: Explicitly specify (prevent "none" algorithm attack)
- Token validation: Check signature, expiration, and claims

**Testing Requirements:**
- Valid token accepted by resource-service
- Expired token rejected
- Invalid signature rejected
- Missing token rejected
- Tampered payload rejected

### POC-Specific Guidelines

1. **Keep It Minimal:** This is NOT production code. Focus on proving the pattern works.

2. **Hardcoded Test Data:** Use hardcoded users and credentials - no database needed for POC.

3. **Separate from Main Codebase:** Do not create these services in apps/ directory. Use a separate poc/ directory or temporary workspace.

4. **Document Everything:** The documentation is more important than the code. Capture lessons learned.

5. **Code Examples:** Include actual working code snippets in the POC documentation for reference.

### File Structure for POC

```
MicroServiceStructure/
├── docs/
│   └── pocs/
│       └── jwt-sso-pattern.md          # POC documentation
├── poc/                                 # Temporary POC workspace
│   ├── poc-auth-service/
│   │   ├── src/
│   │   │   └── auth/
│   │   │       ├── auth.controller.ts
│   │   │       └── auth.service.ts
│   │   └── package.json
│   ├── poc-resource-service/
│   │   ├── src/
│   │   │   ├── auth/
│   │   │   │   └── jwt.strategy.ts
│   │   │   └── protected/
│   │   │       └── protected.controller.ts
│   │   └── package.json
│   └── poc-client/
│       ├── src/
│       │   └── app/
│       │       ├── login/
│       │       └── services/
│       └── package.json
```

### Expected Outputs

1. **Working POC:** Three small applications demonstrating JWT SSO
2. **Documentation:** Comprehensive POC document with code examples
3. **Decision Record:** Updated ADR-001 with HS256 vs RS256 choice and rationale
4. **Lessons Learned:** Any gotchas or issues discovered during POC

### Testing

#### Test Requirements

Since this is a POC, focus on manual testing and documentation rather than automated tests:

1. **Functional Testing:**
   - Login returns valid JWT with correct claims
   - Resource-service validates JWT successfully
   - Angular client can authenticate and call both services
   - Invalid tokens are properly rejected

2. **Security Testing:**
   - Verify token expiration works
   - Verify signature validation prevents tampering
   - Test both HS256 and RS256 approaches

3. **Performance Testing:**
   - Time token generation (HS256 vs RS256)
   - Time token validation (HS256 vs RS256)
   - Document any noticeable differences

#### Verification Steps

1. Start all three POC services (auth, resource, client)
2. Use client to login and receive JWT
3. Verify JWT contains correct claims (decode at jwt.io)
4. Use same JWT to call resource-service protected endpoint
5. Verify resource-service extracts tenantId correctly
6. Test with expired/invalid token to verify rejection
7. Repeat tests for both HS256 and RS256
8. Document findings in docs/pocs/jwt-sso-pattern.md

### Important Notes

1. **This is Exploratory Work:** The goal is learning and validation, not production code.

2. **Delete After Story 1.4:** Once the production auth service is built, the poc/ directory can be deleted (keep documentation).

3. **Focus on Documentation:** The real deliverable is the documentation and ADR update, not the POC code itself.

4. **Inform Production Design:** Lessons learned here will directly inform the implementation of Story 1.4 (Core Authentication Service).

5. **Time-Box the Effort:** This should be a quick validation (2-3 days max), not a full implementation.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-29 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results

*To be filled by QA agent*
